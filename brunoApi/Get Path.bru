meta {
  name: Get Path
  type: http
  seq: 3
}

get {
  url: {{base_url}}/greeting/{{createdUserId}}
  body: json
  auth: inherit
}

script:post-response {
  test("Тест 2 (Часть B): Данные совпадают (Path)", function() {
      expect(res.status).to.equal(200);
      expect(res.body.name).to.equal("Антон");
      expect(res.body.surname).to.equal("Злобко");
  });
  
}

settings {
  encodeUrl: true
  timeout: 0
}
