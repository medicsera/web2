meta {
  name: Get Default
  type: http
  seq: 4
}

get {
  url: {{base_url}}/greeting
  body: none
  auth: inherit
}

script:post-response {
  test("Тест 1: Проверка text = Hello World", function() {
      expect(res.status).to.equal(200);
      expect(res.body.text).to.equal("Hello World");
  });
  
}

settings {
  encodeUrl: true
  timeout: 0
}
